# ISE 333 课程项目技术报告

## 智能导航 UI (Intelligent Navigation UI)

---

**组号：** [填写组号]  
**提交日期：** [填写日期]  
**组长：** 吴梓豪 (R32314019)  
**组员名单：**
- 吴梓豪 R32314019（组长，OR3 + OR4）
- 董劲豪 R32314043（OR1）
- 明子路 R32314001（OR2）
- 万静远 R32314027（OR5）

---

## 目录

1. [UI 设计理念与巧思](#1-ui-设计理念与巧思)
2. [功能使用说明（用户手册）](#2-功能使用说明用户手册)
   - 2.1 [启动程序](#21-启动程序)
   - 2.2 [Basic 功能](#22-basic-功能)
   - 2.3 [OR3：车辆自动对齐方向](#23-or3车辆自动对齐方向)
   - 2.4 [OR4：虚拟街景生成](#24-or4虚拟街景生成)
   - 2.5 [OR1：道路骨架提取](#25-or1道路骨架提取) *(待组员完成)*
   - 2.6 [OR2：缩放与局部可视化](#26-or2缩放与局部可视化) *(待组员完成)*
   - 2.7 [OR5：路径规划](#27-or5路径规划) *(待组员完成)*
3. [技术实现说明](#3-技术实现说明)
   - 3.1 [坐标系统与状态管理](#31-坐标系统与状态管理)
   - 3.2 [图像旋转（手写反向映射）](#32-图像旋转手写反向映射)
   - 3.3 [OR3 道路方向估计](#33-or3-道路方向估计)
   - 3.4 [OR4 光线投射渲染](#34-or4-光线投射渲染)
4. [测试与验证](#4-测试与验证)
5. [组员分工说明](#5-组员分工说明)

---

## 1. UI 设计理念与巧思

### 1.1 设计目标

本 UI 的目标是为用户提供直观的校园地图交互体验，支持智能车辆（IV）的可视化管理、地图测量、以及虚拟视角生成。设计遵循以下原则：

- **单窗口操作**：所有功能集成在一个主窗口中，通过统一的左侧控制面板 + 右侧地图画布完成所有交互，无需切换窗口或使用命令行
- **模式化交互**：不同的操作（加载车辆、测距、OR 功能）通过"模式"（mode）状态机切换，避免误操作和功能冲突
- **即时可视化反馈**：所有操作结果即时显示在地图上，状态栏同步更新提示信息
- **纯手写实现**：所有图像处理算法（旋转、叠加、绘制、渲染）均基于矩阵像素操作手动实现，不依赖高级内置函数

### 1.2 布局设计

```
┌─────────────────────────────────────────────────┐
│ 智能导航 UI                          1200×760  │
├──────────┬──────────────────────────────────────┤
│ 控制面板  │                                      │
│ (320px)  │         地图显示画布                  │
│          │       (880px × 760px)                │
│ 标题      │                                      │
│ ──地图──  │    MapForUI.jpg                      │
│ 旋转滑条  │    + 智能车辆叠加                     │
│ 缩放滑条  │    + 测量标记叠加                     │
│ ──功能──  │    + FOV 锥形标记                     │
│ OR1~5    │                                      │
│ ──智能车─│                                      │
│ 加载/移除 │                                      │
│ 朝向调整  │                                      │
│ 报告位置  │                                      │
│ ──测量──  │                                      │
│ 两点/轨迹 │                                      │
│ ──坐标──  │                                      │
│ X/Y 显示  │                                      │
│ ──状态──  │                                      │
│ 提示信息  │                                      │
└──────────┴──────────────────────────────────────┘
```

### 1.3 设计巧思

1. **模式状态机**：使用 `S.mode` 字段管理当前交互模式（`idle` / `loadIV` / `measure2` / `track` / `or4` / `sketch` 等）。每种模式独立处理鼠标事件，切换模式时自动清理前一个模式的临时数据。例如进入测距模式后点击右键结束、加载车辆模式再次点击按钮取消——避免了需要"确认/取消"按钮的额外 UI 复杂度。

2. **车辆方向可视化**：IV 使用绿色矩形（车身）+ 黄色矩形（车头，占全长 1/4）绘制。用户一眼就能看出车辆朝向，不需要额外标注角度数值。车头/车身颜色对比在旋转后的地图上依然清晰可辨。

3. **"车头朝上"模式的实现思路**：不是旋转每辆车，而是将整个地图绕选定车辆中心反向旋转 90°-angle。这样车辆始终指向上方，同时保持了与其他车辆、地图的道路网络的空间关系。这是地图旋转功能在 OR3 场景下的自然延伸。

4. **OR4 光线投射与主地图联动**：OR4 的相机位置不仅用于生成街景，还通过 FOV 锥形标记叠加回主地图——红色圆点标记相机位置，黄色锥形线标记视野范围。用户可以在主地图上直观看到"相机从哪看、看到哪个方向"。

5. **旋转中心跟随视图**：旋转地图时，旋转中心不是固定在地图中心，而是自动锚定到当前视图显示区域的中心。这样用户先拖拽放大到感兴趣区域再旋转时，旋转不会把该区域甩到视野外。

---

## 2. 功能使用说明（用户手册）

### 2.1 启动程序

1. 将项目文件夹放置于任意位置
2. 打开 MATLAB，将工作目录切换到项目文件夹
3. 在命令行中输入 `main`，按回车

程序将自动加载地图 `MapForUI.jpg`，显示主窗口。

### 2.2 Basic 功能

#### 2.2.1 查看坐标

- **操作**：直接在地图任意位置点击
- **效果**：左侧"坐标"区域实时显示该点的世界坐标（X/Y，单位：米）
- **说明**：世界坐标系原点在地图左下角，X 向右，Y 向上。1 像素 = 1.7 米

#### 2.2.2 拖动和缩放地图

- **拖动**：在地图上按住鼠标左键拖动
- **缩放**：拖动左侧"显示缩放"滑条（范围 1.0x ~ 4.0x）
- **重置**：点击"重置视图"按钮恢复默认

#### 2.2.3 加载智能车辆（IV）

1. 点击左侧"加载车辆"按钮，进入加载模式（状态栏提示确认）
2. 在地图上点击道路上的任意位置
3. **有效点击**：车辆将以 8m×3m 的矩形形式显示在点击位置（为可视性按 3x 放大显示），绿色车身 + 黄色车头指示朝向
4. **无效点击**：（绿地、水面、建筑物等非道路区域）弹出错误提示，不加载车辆
5. 可重复操作加载多辆车辆，每辆车自动递增编号
6. **取消**：再次点击"加载车辆"按钮可退出加载模式

> **道路判断机制**：程序使用 `RoadMask.jpg`（手工绘制的道路遮罩） + 颜色分析（灰度路面特征）双重验证点击点是否在道路上。若组员的 OR1 道路骨架已完成，还将结合骨架数据进一步提高判断精度。

#### 2.2.4 调整车辆朝向

1. 在"选择车辆"下拉列表中选中要调整的车辆
2. 拖动"朝向调整"滑条（范围 0°~360°）
3. 地图上的车辆朝向实时更新
4. 当前角度值显示在滑条下方

> **提示**：如果勾选了"自动对齐道路方向 (OR3)"，加载车辆时朝向会自动对齐道路方向，加载后仍可手动微调。

#### 2.2.5 移除车辆

1. 点击"移除车辆"按钮
2. 在弹出的窗口中选择要移除的车辆（列表显示每辆车的编号、世界坐标和朝向）
3. 点击"移除所选 IV"确认删除
4. 地图刷新，其余车辆不受影响

#### 2.2.6 报告所有车辆位置

- 点击"报告位置"按钮
- 弹出对话框列出所有已加载车辆的世界坐标和朝向角度
- 状态栏同步显示车辆总数

#### 2.2.7 两点测距

1. 点击"两点测距"按钮，进入测距模式
2. 点击地图上第一个点（青色标记出现）
3. 点击地图上第二个点
4. 左侧"距离"标签显示两点间的真实世界直线距离（单位：米）
5. 连线以青色显示在地图上，两端为品红色节点

#### 2.2.8 轨迹长度测量

1. 点击"轨迹测量"按钮，进入轨迹模式
2. **左键**点击地图连续添加轨迹点（每点以品红色标记，点间以青色连线）
3. 左侧"距离"标签实时显示当前轨迹总长度
4. **右键**点击地图结束轨迹输入
5. 状态栏显示最终点数与总长度

#### 2.2.9 清除测量标记

- 点击"清除测量标记"恢复地图的干净视图

#### 2.2.10 旋转地图

1. 拖动"旋转角度"滑条（范围 -180° ~ 180°）
2. 地图绕当前视图中心旋转，地图上的车辆、道路标记、测量线等所有叠加元素同步旋转
3. 旋转角度为 0° 时自动恢复正常显示尺寸；非 0° 时画面自动略微缩小以避免四角空白区域过大

> **实现说明**：旋转通过手写的反向映射 + 最近邻采样算法实现，未使用 MATLAB 内置旋转函数。

### 2.3 OR3：车辆自动对齐方向

**负责组员：** 吴梓豪 (R32314019)

#### 2.3.1 自动对齐道路方向

1. 确保"自动对齐道路方向 (OR3)"复选框已勾选（默认启用）
2. 加载车辆时，车辆朝向将自动与所在道路的走向对齐
3. 效果验证：车辆长边（绿色车身+黄色车头方向）应与道路方向大致一致

**算法说明**：
- 优先使用 OR1 骨架数据：查找距离点击点最近的骨架边，以该边的方向作为车辆朝向
- 若 OR1 数据不可用：对 RoadMask 中点击点附近的白色道路像素做 PCA 主方向分析，以最大方差方向作为道路走向
- 两种方法均以 atan2d 计算角度，角度范围 0°~360°

#### 2.3.2 手动对齐已有车辆

1. 在"选择车辆"下拉列表中选中车辆
2. 点击 OR3 设置窗口中的"对齐到道路（当前车辆）"按钮
3. 车辆朝向自动更新为所在道路的方向

#### 2.3.3 车头始终朝上模式

1. 在"选择车辆"下拉列表中选中一辆已加载的车辆
2. 勾选"车头始终朝上 (OR3)"复选框
3. 地图将围绕选中的车辆中心旋转，使该车辆的长边方向始终指向上方
4. 切换选中的车辆，地图旋转中心和角度自动跟随变化
5. 取消勾选恢复原始朝向

**与手动朝向的关系**：
- 自动对齐仅在**加载时**生效，加载后可用角度滑条手动微调
- 取消"自动对齐"复选框后，加载车辆时使用滑条的当前设置角度
- "车头朝上"模式不影响车辆的存储角度，仅改变显示方式

### 2.4 OR4：虚拟街景生成

**负责组员：** 吴梓豪 (R32314019)

#### 2.4.1 打开 OR4 窗口

- 点击主窗口左侧的"OR4 虚拟街景"按钮
- 弹出 OR4 设置窗口（900×640），左侧为参数控制面板，右侧为街景预览区
- 若已加载过车辆，默认相机位置设为第一辆车的位置，朝向与车辆一致
- 若默认位置在道路上，将自动生成初始街景视图

#### 2.4.2 从已加载车辆设置相机

1. 在"用车辆"下拉列表中选择一辆车
2. 相机位置和 yaw 朝向自动更新为该车辆的位置和朝向
3. 街景自动刷新

#### 2.4.3 点击地图设置相机位置

1. 点击 OR4 窗口中的"在地图上点击设置位置"按钮
2. 主窗口状态栏提示"请点击地图上的道路位置设置相机"
3. 在主地图上点击道路上的任意位置
4. **有效点击**：相机位置更新，街景自动生成，主地图上叠加红色相机标记和黄色 FOV 锥形线
5. **无效点击**：弹出错误提示"该点不在道路上"

#### 2.4.4 调整相机参数

| 参数 | 含义 | 调节方式 |
|------|------|----------|
| X (米) | 相机东西位置 | 数字输入框 |
| Y (米) | 相机南北位置 | 数字输入框 |
| 高度 (米) | 相机离地高度 | 数字输入框（默认 3m） |
| 朝向 yaw | 观察方向：0°=北，90°=东 | 数字输入框；或点击 ±15 按钮微调 |
| 俯仰 pitch | 向下俯视角度 | 数字输入框（默认 10°） |
| 焦距 (px) | 镜头焦距（像素） | 数字输入框（默认 280） |

- 直接修改参数后，需点击"生成街景"按钮查看看效果
- 点击 yaw ±15 按钮可快速预览不同朝向

#### 2.4.5 查看街景结果

- 街景预览显示在 OR4 窗口右侧
- 主地图上叠加红色圆点（相机位置）和黄色锥形（FOV 视野范围，角度由相机参数 `atan(viewW/2 / focalPixel)` 推导，与渲染参数一致）
- 关闭 OR4 窗口时，主地图上的相机标记自动清除

#### 2.4.6 关闭

- 点击"关闭"按钮或直接关闭窗口

### 2.5 OR1：道路骨架提取

*(本节待组员 董劲豪 (R32314043) 完成后补充)*

- [ ] 功能说明
- [ ] 使用方法
- [ ] 截图清单

### 2.6 OR2：缩放与局部可视化

*(本节待组员 明子路 (R32314001) 完成后补充)*

- [ ] 功能说明
- [ ] 使用方法
- [ ] 截图清单

### 2.7 OR5：路径规划

*(本节待组员 万静远 (R32314027) 完成后补充)*

- [ ] 功能说明
- [ ] 使用方法
- [ ] 截图清单

---

## 3. 技术实现说明

### 3.1 坐标系统与状态管理

#### 坐标系约定

| 坐标系 | X/col | Y/row | 原点 | 单位 |
|--------|-------|-------|------|------|
| 像素坐标 | col (1→1404) 向右 | row (1→803) 向下 | 左上角 | px |
| 世界坐标 | X 向右 | Y 向上 | 左下角 | m (1px=1.7m) |

**转换公式**（均手写实现）：

```matlab
% px → world
wx = col * 1.7;
wy = (803 - row) * 1.7;

% world → px
col = wx / 1.7;
row = 803 - wy / 1.7;
```

#### 共享状态 S

所有模块通过 `getappdata(fig, 'S')` / `setappdata(fig, 'S', S)` 读写共享状态。关键字段：

| 字段 | 说明 | 读写 |
|------|------|------|
| `S.mapOrigin` | 原始地图矩阵 | 只读（不可直接修改） |
| `S.mapDisplay` | 当前显示副本（叠加后/旋转后） | 由 refreshDisplay 写入 |
| `S.vehicles` | 车辆数组 [{id,cx,cy,angle,dispScale}] | 各功能写入 |
| `S.mode` | 鼠标交互模式 | 按钮回调写入 |
| `S.rotDeg` | 地图旋转角度 | 滑条回调写入 |
| `S.headUpMode` | 车头朝上开关 | OR3 复选框写入 |
| `S.basicRoadMask` | RoadMask.jpg 读入 | 启动时写入，后续只读 |
| `S.scale` | 比例尺 1.7 | 常量 |

### 3.2 图像旋转（手写反向映射）

**算法**：对输出图像的每个像素，计算其在原始图像中对应的位置，取最近邻像素值。

```
给定：原始图像 img[H×W]，旋转中心 (cx,cy)，旋转角度 θ

1. 计算四个角点绕 (cx,cy) 旋转后的新位置，确定输出画布尺寸 newH×newW
2. 计算偏移量 shiftCol, shiftRow（旋转后最小坐标）
3. 对输出图每个像素 (colOut, rowOut)：
   a. 映射到旋转后空间：rotX = shiftCol + colOut - 1, rotY = shiftRow + rowOut - 1
   b. 反向旋转回原图空间：xOld = (rotX-cx)·cosθ + (rotY-cy)·sinθ + cx
                          yOld = -(rotX-cx)·sinθ + (rotY-cy)·cosθ + cy
   c. 取最近邻：colOld = round(xOld), rowOld = round(yOld)
   d. 如果在原图范围内，采样；否则设为白色背景
```

**关键实现细节**：
- 角点使用 **0.5 偏移**（`0.5, W+0.5` 等），保证旋转后画布尺寸与视觉中心精确对准
- 使用 `ndgrid` 向量化批量计算，避免 MATLAB 慢速双重循环
- 旋转中心支持**任意点**（不仅限于图像中心），用于"车头朝上"模式和视图锚定旋转

### 3.3 OR3 道路方向估计

**方法一：OR1 骨架边分析（精确）**

```
1. 遍历所有 OR1 骨架边
2. 计算点击点到每条边的垂直距离（ptToSeg 函数）
3. 找到最近的边
4. 以该边的方向角 atan2d(dy, dx) 作为道路走向
```

**方法二：RoadMask PCA 主方向（回退）**

```
1. 以点击点为中心，扫描半径 15px 内的 RoadMask 白色像素
2. 收集所有白色像素坐标
3. 计算协方差矩阵：C = [Cxx Cxy; Cxy Cyy]
4. 取最大特征值对应的特征向量方向：θ = 0.5 · atan2(2·Cxy, Cxx-Cyy)
5. 转角度 θ_deg = rad2deg(θ) 作为道路主方向
```

### 3.4 OR4 光线投射渲染

**核心原理**：模拟虚拟相机对地平面拍摄的过程。

```
相机参数：位置 (realX, realY, height)，朝向 yaw，俯仰 pitch，焦距 focalPixel

1. 构造相机基向量（右手坐标系）：
   - forward: 相机观察方向
   - right:   相机右方向
   - up:      相机上方向
   通过 3D 旋转矩阵（R1·R2·R3）计算

2. 对输出视图的每个像素 (vCol, vRow)：
   a. 计算图像平面偏移：
      xPlane = vCol - viewW/2
      yPlane = viewH/2 - vRow
   b. 计算光线方向：
      rayDir = focalPixel·forward + xPlane·right + yPlane·up
   c. 若 rayDir(3) ≥ 0，光线朝上或水平 → 天空
   d. 否则求与地平面交点：
      tGround = -height / rayDir(3)
      groundPt = cameraCenter + tGround·rayDir
   e. 世界坐标 → 地图像素：
      mapCol = groundPt(1) / scale
      mapRow = mapH - groundPt(2) / scale
   f. 若在可见范围内采样，否则灰色
```

**视觉效果**：
- 天空区域 → 浅蓝色 (205, 225, 245)
- 采样地面 → 原地图像素
- 超出范围 → 浅灰色 (235, 235, 235)

---

## 4. 测试与验证

### 4.1 自动化测试

以下测试脚本可在 MATLAB 中独立运行，验证核心算法逻辑：

| 测试脚本 | 覆盖内容 | 结果 |
|----------|----------|------|
| `test_step_a.m` | 地图加载、尺寸、坐标转换一致性 | ✅ |
| `test_step_d.m` | IV 绘制（角度 0/90）、pointInPolygon 射线法、面积守恒 | ✅ |
| `test_step_e.m` | 旋转算法：0°/90°/180°/360° 一致性、旋转中心对齐、性能 | ✅ |
| `test_step_f.m` | 测距算法：单段/L形/对角线/空输入/多点轨迹 | ✅ |
| `test_or4_road_point.m` | OR4 道路点验证：道路点接受、非道路点拒绝、OR1 mask 覆盖 | ✅ |
| `test_or4_debug.m` | OR4 渲染：光线投射统计、采样点 debug 输出 | ✅ |
| `verify_view_center_rotation.m` | 旋转中心与视图中心一致性 | ✅ |
| `smoke_test_main.m` | 端到端冒烟：main() 启动 + 8 项 patch（B1-B3 + S1-S5）行为正确性 | ✅ |

### 4.2 手动测试清单

运行 `main` 后逐项手动验证以下功能：

**Basic 功能：**
- [ ] 地图加载（大小 1404×803，无变形）
- [ ] 点击地图 → X/Y 坐标更新为世界坐标
- [ ] 加载车辆 → 道路点接受 / 非道路点拒绝 + 弹窗
- [ ] 同时显示多辆车（至少 3 辆）
- [ ] 角度滑条调整选中车辆朝向
- [ ] 移除指定车辆，其余不受影响
- [ ] "报告位置"列出所有车辆坐标
- [ ] 两点测距 → 显示距离（米）
- [ ] 轨迹测量 → 多点 + 右键结束 + 总长
- [ ] 旋转地图 → 车辆/标记同步旋转
- [ ] 拖动/缩放地图 → 行为正常

**OR3 功能：**
- [ ] 自动对齐 → 车辆长边沿道路方向
- [ ] 车头朝上 → 地图旋转，车辆指上
- [ ] 切换车辆 → 旋转中心跟随
- [ ] 取消自动对齐 → 手动角度生效
- [ ] 切换 IV 下拉选中 → **角度滑条与标签自动同步到该车朝向**（B2）

**OR4 功能：**
- [ ] 打开弹窗 → 控制面板完整
- [ ] 从车辆选取 → yaw 与朝向一致，且**主窗口 IV 下拉自动跟随同步**（S2）
- [ ] 地图点选设置 → 街景生成 + FOV 标记
- [ ] 非道路点 → 错误提示
- [ ] 选点模式下**右键**点击 → 取消选点并退出 or4 模式（S1）
- [ ] yaw ±15 微调 → 街景刷新
- [ ] 参数修改后点击"生成街景"更新
- [ ] 关闭 → 主地图 FOV 标记清除

---

## 5. 组员分工说明

### 5.1 分工总览

| 组员 | 学号 | 负责部分 | 状态 |
|------|------|----------|------|
| 吴梓豪 | R32314019 | Basic（UI 框架 + 旋转 + 坐标系统）+ OR3 + OR4 | ✅ 已完成 |
| 董劲豪 | R32314043 | Basic（IV 绘制 + 道路验证 + RoadMask）+ OR1 | OR1 ⬜ 待完成 |
| 明子路 | R32314001 | Basic（IV 移除 + 报告位置 + Bresenham + 测试）+ OR2 | OR2 ⬜ 待完成 |
| 万静远 | R32314027 | Basic（测距 + 轨迹测量 + 测量绘图）+ OR5 | OR5 ⬜ 待完成 |

### 5.2 Basic Requirements 详细分工

Basic UI Requirements 是小组共同任务（共 8 项要求）。每位组员的具体贡献如下：

**董劲豪 (R32314043)** — 主要负责 IV 相关基础功能，工作量最大

| 负责内容 | 实现位置 | 说明 |
|----------|----------|------|
| RoadMask.jpg 制作 | `RoadMask.jpg` | 手工在 Photoshop 中沿道路区域绘制白色遮罩，是整个项目道路判断的基础数据 |
| IV 像素绘制算法 | `main.m` drawIV 函数 | 手写旋转矩形绘制：绿色矩形（车身）+ 黄色矩形（车头占全长 1/4），通过旋转矩阵 + 多边形填充实现 |
| IV ID 数字标注 | `main.m` drawIVIdLabel / digitPattern | 手写 5×3 像素数字 0-9 的点阵图案，在每辆车上方标注红色编号 |
| 多边形点判断 | `main.m` pointInPolygon | 手写射线法（Ray Casting）判断像素是否在旋转后的 IV 多边形内 |
| 像素填充工具 | `main.m` stampSquare / fillRectOnImage | 手写矩形区域像素填充，用于绘制节点和标注背景 |
| 道路点颜色验证 | `main.m` isBasicRoadPoint / hasBasicMaskRoadNearby | 基于 RGB 颜色特征（灰度路面、排除绿/蓝区域）判断点击点是否在道路上 |
| IV 加载流程 | `main.m` handleLoadIVClick | 道路验证 → 角度设置 → 车辆入库 → 刷新显示的完整流程 |

**吴梓豪 (R32314019)** — 负责 UI 架构与图像变换核心，工作量次之

| 负责内容 | 实现位置 | 说明 |
|----------|----------|------|
| 主 UI 框架搭建 | `main.m` main 函数 + buildPanel | 单窗口布局设计：uifigure + uiaxes + 左侧 320px 控制面板，所有控件创建和布局 |
| 坐标系统 | `main.m` px2world / world2px | 像素坐标 ↔ 世界坐标转换（原点左下角，Y 轴反转），所有功能共用 |
| 地图旋转算法 | `main.m` rotateMapAroundPoint + getRotationCanvasInfo | 手写反向映射旋转：角点半像素偏移、画布尺寸计算、ndgrid 向量化采样、旋转中心任意点支持 |
| 旋转交互 | `main.m` onRotChanged | 旋转滑条回调：视图中心锚定、旋转后自动缩放、0° 恢复逻辑 |
| 视图漫游 | `main.m` onMouseMove / applyViewLimits / onZoomChanged | 拖拽平移 + 缩放 + 窗口边界 clamp |
| 共享状态设计 | `main.m` 全局状态 S | 设计 S 结构体的字段约定、appdata 读写规范、模式状态机（S.mode） |
| OR 模块接口 | `main.m` onMouseDown 分发 + S.fn | 为 OR1/OR3/OR4/OR5 提供统一的 mode 分发和 fn.refresh/fn.setStatus 接口 |
| 道路点共享函数 | `isRoadPointForUI.m` | 统一 OR4 等模块的道路验证入口，支持 mask 优先级 + 颜色回退 |

**明子路 (R32314001)** — 负责 IV 管理与测试

| 负责内容 | 实现位置 | 说明 |
|----------|----------|------|
| IV 移除功能 | `main.m` onBtnRemoveIV + confirmRemoveIV | 弹窗列出所有车辆 → 选中确认删除 → 仅移除指定 IV，其他不受影响 |
| 报告 IV 位置 | `main.m` onBtnReportIV | 遍历所有车辆，格式化输出编号/坐标/朝向，uialert 弹窗显示 |
| Bresenham 直线算法 | `main.m` bresenham | 手写经典 Bresenham 画线算法，用于测量连线显示 |
| 测试脚本编写 | `test_step_a.m` / `test_step_d.m` / `test_step_f.m` | 自动化验证：地图加载与坐标转换、IV 绘制与道路验证、测量算法等 |

**万静远 (R32314027)** — 负责测量功能

| 负责内容 | 实现位置 | 说明 |
|----------|----------|------|
| 两点测距 | `main.m` onBtnMeasure2 + handleMeasure2Click | 两点击模式：记录两点 → 欧几里得距离 × 比例尺 → 显示世界距离（米） |
| 轨迹测量 | `main.m` onBtnTrack + handleTrackClick + computeTrackLength | 多点击模式：左键追加 / 右键结束 → 折线累加总长 |
| 测量标记绘制 | `main.m` drawMeasurement | 青色 Bresenham 连线 + 品红 stampSquare 节点，叠加到复合图上并同步旋转 |
| 测量清除 | `main.m` onBtnClearMeasure | 清除测量点数据 + 恢复未标记视图 |

### 5.3 Optional Requirements 分工

| 组员 | OR 任务 | 文件名 | 状态 |
|------|---------|--------|------|
| 董劲豪 | OR1 道路骨架提取 | `or1_skeleton.m` | ⬜ 待完成 |
| 明子路 | OR2 缩放与局部可视化 | `or2_*.m`（待创建） | ⬜ 待完成 |
| 吴梓豪 | OR3 车辆自动对齐方向 | `or3_auto_align.m` | ✅ 已完成 |
| 吴梓豪 | OR4 虚拟街景生成 | `or4_street_view.m` | ✅ 已完成 |
| 万静远 | OR5 路径规划 | `or5_*.m`（待创建） | ⬜ 待完成 |

### 5.4 代码文件归属

| 文件 | 负责人 | 说明 |
|------|--------|------|
| `main.m` | 共同维护（吴梓豪主导） | 主 UI 框架、Basic 功能入口、OR 按钮与事件分发 |
| `or3_auto_align.m` | 吴梓豪 | OR3 道路方向估计 + 车头朝上旋转 |
| `or4_street_view.m` | 吴梓豪 | OR4 虚拟街景：相机模型 + 光线投射渲染 |
| `isRoadPointForUI.m` | 吴梓豪 | 道路点验证共享函数 |
| `RoadMask.jpg` | 董劲豪 | 手工道路遮罩 |
| `or1_skeleton.m` | 董劲豪 | OR1 道路骨架工具 |
| `or2_*.m` | 明子路 | OR2（待创建） |
| `or5_*.m` | 万静远 | OR5（待创建） |
| `test_step_a/d/e/f.m` | 明子路/吴梓豪 | 自动化测试脚本 |
| `test_or4_*.m` | 吴梓豪 | OR4 专项测试脚本 |

---

> **提交注意事项（摘自课程要求）：**
> 1. 将全部 `.m` 文件 + 图像资源打包为 `.zip`
> 2. 本报告与代码压缩包一起作为邮件附件发送给 Hao Li 教授
> 3. 邮件正文需包含附件说明 + 所有组员完整名单
> 4. **发送后务必等待教授回复确认 (Acknowledgement of reception)**，未收到需及时跟进
