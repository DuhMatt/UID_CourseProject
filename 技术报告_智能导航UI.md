# ISE 333 课程项目技术报告

## 智能导航 UI (Intelligent Navigation UI)

---

**组号：** [填写组号]  
**提交日期：** 2026-06-19  
**组长：** 吴梓豪 (R32314019)  
**组员名单：**
- 吴梓豪 R32314019（组长，Basic 框架 + OR3 + OR4）
- 董劲豪 R32314043（Basic IV 绘制 + OR1）
- 明子路 R32314001（Basic IV 管理 + OR2）
- 万静远 R32314027（Basic 测量 + OR5）

---

## 目录

1. [UI 设计理念与巧思](#1-ui-设计理念与巧思)
2. [功能使用说明（用户手册）](#2-功能使用说明用户手册)
   - 2.1 [启动程序](#21-启动程序)
   - 2.2 [Basic 功能](#22-basic-功能)
   - 2.3 [OR1：道路骨架提取](#23-or1道路骨架提取)
   - 2.4 [OR2：缩放与局部可视化](#24-or2缩放与局部可视化)
   - 2.5 [OR3：车辆自动对齐方向](#25-or3车辆自动对齐方向)
   - 2.6 [OR4：虚拟街景生成](#26-or4虚拟街景生成)
   - 2.7 [OR5：路径规划](#27-or5路径规划)
3. [技术实现说明](#3-技术实现说明)
   - 3.1 [坐标系统与状态管理](#31-坐标系统与状态管理)
   - 3.2 [图像旋转（手写反向映射）](#32-图像旋转手写反向映射)
   - 3.3 [OR3 道路方向估计](#33-or3-道路方向估计)
   - 3.4 [OR4 光线投射渲染](#34-or4-光线投射渲染)
   - 3.5 [OR5 图搜索算法](#35-or5-图搜索算法)
4. [测试与验证](#4-测试与验证)
5. [组员分工说明](#5-组员分工说明)

---

## 1. UI 设计理念与巧思

### 1.1 设计目标

本 UI 的目标是为用户提供直观的校园地图交互体验，支持智能车辆（IV）的可视化管理、地图测量、以及各 OR 扩展功能。设计遵循以下原则：

- **单窗口 + 模块化**：所有功能集成在一个主窗口中，通过统一的左侧控制面板完成交互。每个 OR 功能作为独立 `.m` 模块文件接入，按钮点击触发，`main.m` 仅保留入口和事件分发。
- **模式化交互**：不同的操作（加载车辆、测距、OR 功能）通过"模式"（`S.mode`）状态机切换。每次只有一个模式激活，避免误操作和功能冲突。
- **即时可视化反馈**：所有操作结果即时显示在地图上，状态栏同步更新提示信息。
- **纯手写实现**：所有图像处理算法（旋转、叠加、绘制、渲染、图搜索）均基于矩阵像素操作手动实现，不依赖高级 MATLAB 内置函数。

### 1.2 布局设计

```
┌─────────────────────────────────────────────────┐
│ 智能导航 UI                           1240×760  │
├──────────┬──────────────────────────────────────┤
│ 控制面板  │                                      │
│ (360px)  │         地图显示画布                  │
│ 可折叠    │       (880px × 760px)                │
│          │                                      │
│ 标题      │    MapForUI.jpg                      │
│ ──地图──  │    + 智能车辆叠加                     │
│ 旋转滑条  │    + 道路骨架叠加                     │
│ 缩放滑条  │    + 测量/路径标记                    │
│ ──功能──  │    + FOV 锥形标记                     │
│ OR1~5    │                                      │
│ ──智能车─│                                      │
│ 加载/移除 │                                      │
│ 朝向调整  │                                      │
│ OR3复选框 │                                      │
│ 报告位置  │                                      │
│ ──测量──  │                                      │
│ 两点/轨迹 │                                      │
│ ──坐标──  │                                      │
│ X/Y 显示  │                                      │
│ ──状态──  │                                      │
│ 提示信息  │                                      │
├──────────┴──────────────────────────────────────┤
│             状态栏                               │
└─────────────────────────────────────────────────┘
```

### 1.3 设计巧思

1. **模式状态机**：使用 `S.mode` 字段统一管理当前交互模式（`idle` / `loadIV` / `measure2` / `track` / `or2` / `or4` / `or5_start` / `or5_end` / `sketch` / `erase` 等）。每种模式独立处理鼠标事件，切换模式时自动清理临时数据。例如测距模式可右键结束、加载车辆模式再次点击按钮取消——避免了"确认/取消"弹窗的额外 UI 复杂度。

2. **车辆方向可视化**：IV 使用绿色矩形（车身）+ 黄色矩形（车头，占全长 1/4）绘制。用户一眼就能看出车辆朝向，不需要额外标注角度数值。车头/车身颜色对比在旋转后的地图上依然清晰可辨。

3. **"车头朝上"模式的实现思路**：不是旋转每辆车，而是将整个地图绕选定车辆中心反向旋转 90°−angle。这样车辆始终指向上方，同时保持与其他车辆、道路网络的空间关系。这是地图旋转功能在 OR3 场景下的自然延伸。

4. **OR4 光线投射与主地图联动**：OR4 的相机位置不仅用于生成街景，还通过 FOV 锥形标记叠加回主地图——红色圆点标记相机位置，黄色锥形线标记视野范围。用户可以在主地图上直观看到"相机从哪看、看到哪个方向"。

5. **旋转中心跟随视图**：旋转地图时，旋转中心自动锚定到当前视图显示区域的中心。用户先拖拽放大到感兴趣区域再旋转时，旋转不会把该区域甩到视野外。

6. **面板折叠**：控制面板可通过左上角折叠按钮收窄至 28px，地图画布自动扩展填满窗口，在查看大范围地图时提供最大视野。

7. **OR 模块化架构**：每个 OR 功能作为独立 `.m` 文件，通过统一的 action-dispatch 接口接入 `main.m`。`main.m` 对每个 OR 只做三件事：按钮接线、`onMouseDown` mode 分发、最小状态桥梁（`S.fn.refresh` / `S.fn.setStatus` 函数句柄）。所有业务逻辑完全在 OR 模块内闭环，组员可以独立开发互不冲突。

---

## 2. 功能使用说明（用户手册）

### 2.1 启动程序

1. 将项目文件夹放置于任意位置
2. 打开 MATLAB，将工作目录切换到项目文件夹
3. 在命令行中输入 `main`，按回车

程序将自动加载地图 `MapForUI.jpg` 和道路遮罩 `RoadMask.jpg`，显示主窗口。

### 2.2 Basic 功能

#### 2.2.1 查看坐标

- **操作**：直接在地图任意位置点击
- **效果**：左侧"坐标"区域实时显示该点的世界坐标（X/Y，单位：米）
- **说明**：世界坐标系原点在地图左下角，X 向右，Y 向上。1 像素 = 1.7 米

#### 2.2.2 拖动和缩放地图

- **拖动**：在 idle 模式下，按住鼠标左键拖动
- **缩放**：拖动左侧"显示缩放"滑条（范围 0.5x ~ 4.0x）
- **重置**：点击"重置视图"按钮恢复默认（缩放 1.0x，旋转 0°）

#### 2.2.3 加载智能车辆（IV）

1. 点击左侧"加载车辆"按钮，进入加载模式（状态栏提示确认）
2. 在地图上点击道路上的任意位置
3. **有效点击**：车辆以 8m×3m 矩形显示在点击位置（为可视性按 3x 放大），绿色车身 + 黄色车头指示朝向
4. **无效点击**：弹出错误提示"该点不在道路上"，不加载车辆
5. 可重复操作加载多辆车辆，每辆车自动递增编号
6. **取消**：再次点击"加载车辆"按钮退出加载模式

> **道路判断机制**：程序使用 `RoadMask.jpg` + OR1 骨架数据 + 颜色分析三重验证点击点是否在道路上。

#### 2.2.4 调整车辆朝向

1. 在"选择车辆"下拉列表中选中要调整的车辆
2. 拖动"朝向调整"滑条（范围 0° ~ 360°）
3. 地图上的车辆朝向实时更新

> 勾选"自动对齐道路方向 (OR3)"时，加载车辆会自动对齐道路方向，加载后仍可手动微调。

#### 2.2.5 移除车辆

1. 点击"移除车辆"按钮
2. 在弹出的窗口中选择要移除的车辆（列表显示每辆车的编号、世界坐标和朝向）
3. 点击"移除所选 IV"确认
4. 地图刷新，其余车辆不受影响

#### 2.2.6 报告所有车辆位置

- 点击"报告位置"按钮，弹出对话框列出所有已加载车辆的世界坐标和朝向角度

#### 2.2.7 两点测距

1. 点击"两点测距"按钮
2. 点击地图上第一个点（青色标记出现）
3. 点击地图上第二个点
4. 左侧显示两点间的真实世界直线距离（米）

#### 2.2.8 轨迹长度测量

1. 点击"轨迹测量"按钮
2. **左键**点击地图添加轨迹点（品红节点 + 青色连线）
3. 左侧实时显示当前轨迹总长度
4. **右键**点击结束轨迹输入

#### 2.2.9 旋转地图

- 拖动"旋转角度"滑条（范围 -180° ~ 180°），地图绕当前视图中心旋转
- 车辆、道路标记、测量线等所有叠加元素同步旋转
- 旋转角度为 0° 时恢复正常

#### 2.2.10 折叠面板

- 点击左上角 `<` / `>` 按钮折叠或展开左侧控制面板

### 2.3 OR1：道路骨架提取

**负责组员：** 董劲豪 (R32314043)  
**文件：** `or1_skeleton.m`

OR1 提供手动提取和可视化道路骨架的功能，操作直接在主地图上进行。

#### 操作方法

1. 点击主窗口左侧的 **"OR1 道路骨架"** 按钮，弹出 OR1 控制窗口
2. 控制窗口提供以下按钮：
   - **绘制骨架**：进入绘制模式，左键点击地图放置节点，连续点击形成折线；右键结束当前折线
   - **擦除**：进入擦除模式，点击骨架线段可删除该段
   - **清除全部**：清除所有已绘制的骨架
   - **道路宽度**：通过滑条调整骨架"加肉"的像素半宽（默认 2px）
   - **完成提取**：将当前骨架固化为道路遮罩（蓝色半透明覆盖层）
   - **关闭**：关闭 OR1 窗口
3. 绘制过程中，骨架以红色实线 + 黄色节点实时显示在主地图上
4. 完成提取后，道路区域以蓝色半透明覆盖层显示
5. 骨架数据自动存入共享状态 `S.sk.nodes` / `S.sk.edges`，供 OR3 自动对齐等功能使用

### 2.4 OR2：缩放与局部可视化

**负责组员：** 明子路 (R32314001)  
**文件：** `or2_local_view.m`

OR2 提供 IV 显示缩放调节和以车辆为中心的圆形局部地图查看功能。

#### 操作方法

1. 点击主窗口左侧的 **"OR2 局部视图"** 按钮，弹出 OR2 控制窗口
2. OR2 窗口包含以下控件：
   - **选择 IV**：下拉列表选择要查看的车辆
   - **IV显示缩放倍数**：滑条（1x ~ 10x）调整主地图上车辆的显示大小
   - **圆形局部地图半径**：滑条（20m ~ 500m）设置圆形视窗半径
   - **显示圆形局部地图**：立即刷新预览
   - **恢复完整主地图**：恢复主地图的全幅显示
3. **快捷操作**：在主地图上点击车辆附近，OR2 自动选中最近的车辆并更新局部视图
4. 预览窗口显示以选中车辆为中心的圆形局部地图，红色圆边界 + 黄色十字标记中心
5. 半径严格以世界坐标米为单位转换（`radiusM / 1.7` → 像素），确保显示范围与指定范围精确吻合

### 2.5 OR3：车辆自动对齐方向

**负责组员：** 吴梓豪 (R32314019)  
**文件：** `or3_auto_align.m`

OR3 提供车辆加载时的自动道路方向对齐和"车头始终朝上"显示模式。

#### 2.5.1 自动对齐道路方向

1. 确保"自动对齐道路方向 (OR3)"复选框已勾选（默认启用）
2. 加载车辆时，车辆朝向将自动与所在道路的走向对齐
3. 车辆长边（绿色车身 + 黄色车头方向）与道路方向大致一致

**算法**：优先使用 OR1 骨架数据（最近边方向角 atan2d）；骨架不可用时分析 RoadMask 中附近道路像素的 PCA 主方向。

#### 2.5.2 手动对齐已有车辆

1. 在"选择车辆"下拉列表中选中车辆
2. 点击 **"OR3 车辆朝向"** 按钮打开 OR3 设置窗口
3. 点击"对齐到道路（当前车辆）"

#### 2.5.3 车头始终朝上模式

1. 选中一辆已加载的车辆
2. 勾选"车头始终朝上 (OR3)"复选框
3. 地图围绕选中车辆中心旋转，车辆长边始终指向上方
4. 切换选中车辆 → 旋转中心和角度自动跟随
5. 取消勾选恢复原始朝向

### 2.6 OR4：虚拟街景生成

**负责组员：** 吴梓豪 (R32314019)  
**文件：** `or4_street_view.m`

OR4 基于光柆投射原理，从任意道路点生成虚拟街景视图。

#### 操作方法

1. 点击 **"OR4 虚拟街景"** 按钮，弹出 OR4 设置窗口（900×640）
2. 设置相机来源：
   - **从已加载车辆**：在下拉列表选择车辆，相机自动获取其位置和朝向
   - **点击地图**：点击"在地图上点击设置位置"按钮，然后在主地图上点击道路任意位置
3. 调整相机参数（可直接输入数字）：
   - **X/Y (米)**：相机位置
   - **高度 (米)**：相机离地高度（默认 3m）
   - **朝向 yaw**：观察方向（0°=北，90°=东），可点击 ±15 按钮微调
   - **俯仰 pitch**：向下俯视角度（默认 10°）
   - **焦距 (px)**：镜头焦距（默认 280）
4. 点击"生成街景"按钮查看效果
5. 街景预览显示在右侧，主地图叠加红色相机标记 + 黄色 FOV 锥形
6. 关闭 OR4 窗口时，主地图上的标记自动清除

### 2.7 OR5：路径规划

**负责组员：** 万静远 (R32314027)  
**文件：** `or5_path_planning.m`

OR5 在道路网络上计算并可视化两点之间的最短路径。

#### 操作方法

1. 点击主窗口左侧的 **"OR5 路径规划"** 按钮，弹出 OR5 控制窗口
2. OR5 窗口包含以下按钮：
   - **设置起点**：进入起点选择模式，点击地图上的道路位置
   - **设置终点**：进入终点选择模式，点击地图上的道路位置
   - **计算路径**：执行最短路径搜索
   - **清除路径**：清除当前路径显示
   - **关闭**：关闭 OR5 窗口
3. 点击地图时自动吸附到最近的道路像素点
4. 路径计算使用**手写 BFS 图搜索算法**（等权网格最短路径，等效于 Dijkstra 特例）
5. 计算结果以绿色方块绘制在主地图上（起点蓝色、终点红色）
6. 路径总长度（米）显示在 OR5 窗口和主状态栏中

---

## 3. 技术实现说明

### 3.1 坐标系统与状态管理

#### 坐标系约定

| 坐标系 | X/col | Y/row | 原点 | 单位 |
|--------|-------|-------|------|------|
| 像素坐标 | col (1→1404) 向右 | row (1→803) 向下 | 左上角 | px |
| 世界坐标 | X 向右 | Y 向上 | 左下角 | m (1px=1.7m) |

#### 共享状态 S

所有模块通过 `getappdata(fig, 'S')` / `setappdata(fig, 'S', S)` 读写共享状态。关键字段：

| 字段 | 说明 | 读写 |
|------|------|------|
| `S.mapOrigin` | 原始地图矩阵 | 只读 |
| `S.mapDisplay` | 当前显示副本（叠加/旋转后） | refreshDisplay 写入 |
| `S.vehicles` | 车辆数组 [{id,cx,cy,angle,dispScale}] | 各功能写入 |
| `S.mode` | 鼠标交互模式 | 按钮/OR 模块写入 |
| `S.rotDeg` | 地图旋转角度 | 滑条回调写入 |
| `S.headUpMode` | 车头朝上开关 | OR3 写入 |
| `S.basicRoadMask` | RoadMask.jpg 读入 | 启动时写入，后续只读 |
| `S.sk.nodes/edges` | OR1 骨架数据 | OR1 写入 |
| `S.roadMask` | OR1 道路遮罩 | OR1 写入 |
| `S.fn.refresh` | 刷新显示函数句柄 | main.m 提供 |
| `S.fn.setStatus` | 状态栏更新句柄 | main.m 提供 |
| `S.scale` | 比例尺 1.7 m/px | 常量 |

### 3.2 图像旋转（手写反向映射）

**算法**：对输出图像的每个像素，计算其原始图像对应位置，取最近邻采样。

```
给定：原始图像 img[H×W]，旋转中心 (cx,cy)，旋转角度 θ

1. 计算四个角点绕 (cx,cy) 旋转后的位置，确定画布尺寸 newH×newW
2. 计算偏移量 shiftCol, shiftRow
3. 对输出图每个像素 (colOut, rowOut)：
   a. 映射到旋转后空间：rotX = shiftCol + colOut - 1
   b. 反向旋转回原图：
      xOld = (rotX-cx)·cosθ + (rotY-cy)·sinθ + cx
      yOld = -(rotX-cx)·sinθ + (rotY-cy)·cosθ + cy
   c. 最近邻采样
```

实现特点：角点半像素偏移（0.5, W+0.5），ndgrid 向量化批量计算，支持任意旋转中心。

### 3.3 OR3 道路方向估计

**方法一：OR1 骨架边分析（精确）**
- 遍历所有 OR1 骨架边，计算点击点到边的垂直距离
- 找最近的边，以 `atan2d(dy, dx)` 作为道路方向

**方法二：RoadMask PCA 主方向（回退）**
- 以点击点为中心，扫描半径 15px 内的白色像素
- 计算协方差矩阵，取最大特征值对应特征向量方向
- `θ = 0.5 · atan2(2·Cxy, Cxx−Cyy)`

### 3.4 OR4 光线投射渲染

模拟虚拟相机对地平面拍摄：

```
相机参数：位置 (X,Y,height)，朝向 yaw，俯仰 pitch，焦距 focalPixel

对输出视图每个像素：
1. 计算光线方向：rayDir = focalPixel·forward + xPlane·right + yPlane·up
2. 求与地平面交点：t = -height / rayDir(3)
3. 交点世界坐标 → 地图像素坐标
4. 有效范围内采样，范围外灰色
5. 天空（光线朝上）→ 浅蓝色
```

### 3.5 OR5 图搜索算法

**手写 BFS（在等权图上等效于 Dijkstra）：**

```
输入：道路遮罩栅格，起点像素 (sr,sc)，终点像素 (er,ec)
输出：最短路径像素序列

1. 初始化：队列 queue[head..tail]，prev 数组，visited 标记
2. BFS 主循环：
   - 出队当前像素，检查四个邻居（上下左右）
   - 跳过非道路像素和已访问像素
   - 标记访问，记录前驱，入队
3. 若到达终点，回溯 prev 数组构建路径
4. 若队列耗尽，起点和终点无连通路径
```

算法完全手写，未使用 MATLAB `graph` / `shortestpath` 等高级图论函数。

---

## 4. 测试与验证

### 4.1 自动化测试脚本

| 测试脚本 | 覆盖内容 |
|----------|----------|
| `test_step_a.m` | 地图加载、尺寸、坐标转换一致性 |
| `test_step_b.m` | OR1 骨架绘制基础测试 |
| `test_step_c.m` | OR1 道路遮罩生成 |
| `test_step_d.m` | IV 绘制（角度 0/90）、多边形点判断 |
| `test_step_e.m` | 旋转算法：0°/90°/180°/360° 一致性 |
| `test_step_f.m` | 测距算法：单段/L形/对角线/多点轨迹 |
| `test_or4_debug.m` | OR4 光线投射统计与 Debug |
| `test_or4_road_point.m` | OR4 道路点验证 |
| `test_or5_popup.m` | OR5 弹窗与路径搜索 |
| `verify_view_center_rotation.m` | 旋转中心与视图一致性 |
| `smoke_test_main.m` | 端到端冒烟测试 |

### 4.2 手动检查清单

运行 `main` 后逐项验证：

**Basic 功能：**
- [ ] 地图加载（1404×803，无变形）
- [ ] 点击地图 → X/Y 坐标显示世界坐标
- [ ] 加载车辆 → 道路点接受 / 非道路点拒绝 + 弹窗提示
- [ ] 同时显示多辆车（≥3 辆）
- [ ] 角度滑条调整选中车辆朝向
- [ ] 移除指定车辆，其余不受影响
- [ ] "报告位置"列出所有车辆坐标
- [ ] 两点测距 → 显示世界距离（m）
- [ ] 轨迹测量 → 多点 + 右键结束 + 总长
- [ ] 旋转地图 → 车辆/标记同步旋转
- [ ] 拖动/缩放地图 → 行为正常

**OR1 道路骨架：**
- [ ] 弹窗显示完整控制按钮
- [ ] 左键绘制折线，右键结束
- [ ] 红色骨架线 + 黄色节点实时显示
- [ ] 擦除模式可删除线段
- [ ] 完成提取后蓝色道路遮罩显示

**OR2 局部视图：**
- [ ] 弹窗显示完整控件（IV下拉、缩放/半径滑条）
- [ ] 缩放滑条改变车辆显示大小
- [ ] 半径滑条改变圆形视窗范围
- [ ] 圆形严格以车辆为中心，半径与指定米数吻合
- [ ] 点击主地图自动选中最近车辆

**OR3 自动对齐：**
- [ ] 自动对齐 → 车辆长边沿道路方向
- [ ] 车头朝上 → 地图旋转，车辆指上
- [ ] 切换车辆 → 旋转中心跟随

**OR4 虚拟街景：**
- [ ] 打开弹窗 → 控制面板完整
- [ ] 从车辆选取 → yaw 与朝向一致
- [ ] 地图点选 → 街景生成 + FOV 标记
- [ ] 非道路点 → 错误提示
- [ ] yaw ±15 微调 → 街景刷新
- [ ] 关闭 → 主地图 FOV 标记清除

**OR5 路径规划：**
- [ ] 弹窗显示完整按钮
- [ ] 设置起点 → 吸附到最近道路点
- [ ] 设置终点 → 吸附到最近道路点
- [ ] 计算路径 → 绿色最短路径显示
- [ ] 路径长度以米为单位显示

---

## 5. 组员分工说明

### 5.1 分工总览

| 组员 | 学号 | 负责部分 | 状态 |
|------|------|----------|:----:|
| 吴梓豪 | R32314019 | Basic（UI 框架 + 旋转 + 坐标系统 + OR 模块接口）+ OR3 + OR4 | ✅ |
| 董劲豪 | R32314043 | Basic（IV 绘制 + 道路验证 + RoadMask）+ OR1 | ✅ |
| 明子路 | R32314001 | Basic（IV 移除 + 报告位置 + Bresenham）+ OR2 | ✅ |
| 万静远 | R32314027 | Basic（测距 + 轨迹测量 + 测量绘图）+ OR5 | ✅ |

### 5.2 Basic Requirements 详细分工

Basic UI Requirements 是小组共同任务（15 项要求）。每位组员的具体贡献：

**吴梓豪 (R32314019)** — UI 架构与图像变换核心

| 负责内容 | 实现位置 | 说明 |
|----------|----------|------|
| 主 UI 框架 | `main.m` main 函数 + buildPanel | uifigure + uiaxes + 360px 控制面板，折叠/展开按钮，所有控件布局 |
| 坐标系统 | `main.m` px2world / world2px | 像素 ↔ 世界坐标转换，左下角原点 |
| 地图旋转算法 | `main.m` rotateMapAroundPoint + getRotationCanvasInfo | 手写反向映射旋转，角点半像素偏移，ndgrid 向量化 |
| 视图漫游 | `main.m` onMouseMove / applyViewLimits / onZoomChanged | 拖拽平移 + 缩放 + 窗口边界 clamp |
| 共享状态与模块接口 | `main.m` S 结构体 + S.fn + onMouseDown 分发 | 为所有 OR 模块提供统一的状态读写和事件分发 |
| 道路点共享函数 | `isRoadPointForUI.m` | 统一道路验证入口，支持 mask 优先级 + 颜色回退 |

**董劲豪 (R32314043)** — IV 绘制与道路基础数据

| 负责内容 | 实现位置 | 说明 |
|----------|----------|------|
| RoadMask.jpg 制作 | `RoadMask.jpg` | 手工在 Photoshop 中绘制道路区域白色遮罩 |
| IV 像素绘制 | `main.m` drawIV | 手写旋转矩形：绿色车身 + 黄色车头（占全长 1/4），旋转矩阵 + 多边形填充 |
| IV ID 标注 | `main.m` drawIVIdLabel / digitPattern | 手写 5×3 像素数字 0–9 点阵 |
| 多边形点判断 | `main.m` pointInPolygon | 手写射线法（Ray Casting） |
| 像素填充工具 | `main.m` stampSquare / fillRectOnImage | 矩形区域像素填充 |
| 道路点颜色验证 | `main.m` isBasicRoadPoint / hasBasicMaskRoadNearby | RGB 颜色特征判断 |
| IV 加载流程 | `main.m` handleLoadIVClick | 道路验证 → 角度设置 → 车辆入库 → 刷新 |

**明子路 (R32314001)** — IV 管理与测试

| 负责内容 | 实现位置 | 说明 |
|----------|----------|------|
| IV 移除功能 | `main.m` onBtnRemoveIV + confirmRemoveIV | 弹窗选择 → 确认删除 → 仅移除指定车辆 |
| 报告 IV 位置 | `main.m` onBtnReportIV | 遍历车辆，格式化输出，uialert 弹窗 |
| Bresenham 直线 | `main.m` bresenham | 手写经典 Bresenham 算法 |
| 测试脚本 | `test_step_a/d/f.m` | 地图加载、IV 绘制、测量算法自动验证 |

**万静远 (R32314027)** — 测量功能

| 负责内容 | 实现位置 | 说明 |
|----------|----------|------|
| 两点测距 | `main.m` onBtnMeasure2 + handleMeasure2Click | 两点击 → 欧几里得距离 × 比例尺 |
| 轨迹测量 | `main.m` onBtnTrack + handleTrackClick + computeTrackLength | 多点左键追加 / 右键结束 → 折线累加 |
| 测量标记绘制 | `main.m` drawMeasurement | 青色 Bresenham 连线 + 品红节点，叠加后同步旋转 |
| 测量清除 | `main.m` onBtnClearMeasure | 清除数据 + 恢复视图 |

### 5.3 Optional Requirements 分工

| 组员 | OR 任务 | 文件名 | 状态 |
|------|---------|--------|:----:|
| 董劲豪 | OR1 道路骨架提取 | `or1_skeleton.m` | ✅ |
| 明子路 | OR2 缩放与局部可视化 | `or2_local_view.m` | ✅ |
| 吴梓豪 | OR3 车辆自动对齐方向 | `or3_auto_align.m` | ✅ |
| 吴梓豪 | OR4 虚拟街景生成 | `or4_street_view.m` | ✅ |
| 万静远 | OR5 路径规划 | `or5_path_planning.m` | ✅ |

### 5.4 代码文件归属

| 文件 | 负责人 | 说明 |
|------|--------|------|
| `main.m` | 共同维护（吴梓豪主导） | 主 UI 框架、Basic 功能入口、OR 按钮与事件分发 |
| `or1_skeleton.m` | 董劲豪 | OR1 道路骨架工具（弹窗 + 绘制/擦除/完成提取） |
| `or2_local_view.m` | 明子路 | OR2 IV 缩放与圆形局部地图（弹窗 + 缩放/半径滑条 + 预览） |
| `or3_auto_align.m` | 吴梓豪 | OR3 道路方向估计 + 自动对齐 + 车头朝上旋转 |
| `or4_street_view.m` | 吴梓豪 | OR4 虚拟街景：相机模型 + 光线投射渲染 + FOV 标记 |
| `or5_path_planning.m` | 万静远 | OR5 路径规划：手写 BFS + 道路点吸附 + 路径可视化 |
| `isRoadPointForUI.m` | 吴梓豪 | 道路点验证共享函数 |
| `generateRoadMaskFromMap.m` | 董劲豪 | 从骨架自动生成道路遮罩 |
| `RoadMask.jpg` | 董劲豪 | 手工道路遮罩（Photoshop） |
| `MapForUI.jpg` | 课程提供 | 校园地图底图 |
| `test_step_a/d/e/f.m` | 明子路/吴梓豪 | 自动化测试脚本 |
| `test_or4_*.m` | 吴梓豪 | OR4 专项测试 |

### 5.5 合规声明确认

| 检查项 | 状态 |
|--------|:----:|
| 仅使用 MATLAB | ✅ |
| `main` 直接启动，无需路径配置 | ✅ |
| 所有操作通过 UI 完成 | ✅ |
| 未使用禁止的高级内置函数（patch/imrotate/graph 等） | ✅ |
| 坐标原点为左下角，Y 轴向上 | ✅ |
| 各 OR 独立文件，模块化清晰 | ✅ |
| RoadMask.jpg 标注为离线辅助工具 | ✅ |

---

> **提交注意事项：**
> 1. 将全部 `.m` 文件 + 图像资源（`MapForUI.jpg` + `RoadMask.jpg`）打包为 `.zip`
> 2. 本报告与代码压缩包一起作为邮件附件发送给 Hao Li 教授
> 3. 邮件正文需包含附件说明 + 所有组员完整名单（姓名 + 学号）
> 4. **发送后务必等待教授回复确认 (Acknowledgement of reception)**，未收到需及时跟进
